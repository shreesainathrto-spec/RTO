rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Authenticated users can read any file
    match /{allPaths=**} {
      allow read: if request.auth != null;
    }

    // Per-customer documents: authenticated write, max 10 MB
    match /customers/{customerId}/docs/{filename} {
      allow write: if request.auth != null
                   && request.resource.size < 10 * 1024 * 1024;
    }

    // Customer attachments: authenticated write, max 10 MB
    match /customers/{customerId}/attachments/{filename} {
      allow write: if request.auth != null
                   && request.resource.size < 10 * 1024 * 1024;
      allow delete: if request.auth != null;
    }

    // Task attachments: authenticated write, max 10 MB
    match /tasks/{taskId}/attachments/{filename} {
      allow write: if request.auth != null
                   && request.resource.size < 10 * 1024 * 1024;
      allow delete: if request.auth != null;
    }
  }
}
