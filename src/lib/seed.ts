// One-time seed: cleared out to prevent any dummy data initialization
import { doc, setDoc } from "firebase/firestore";
import { db } from "./firebase";

export async function seedInitialData(): Promise<void> {
  // Cleared out to prevent hardcoded client generation
}
